package arbolesBinarios;

public class Ej80_AlturasMaxMin {

	// En un arbol binario de n elementos
	// La altura maxima es n - 1, ya que en el peor de los casos todos los elementos estaran 
	// conectados directamente A->B->C->D->E->...->Z
	
	// La altura minima seria de log(n), ya que cada nodo (excepto las hojas) se va dividiendo
	// en 2 nodos.
}
