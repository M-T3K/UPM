package colas_monticulos_heaps;

public class Ej100_ComplejidadColas {

	// Complejidad Colas
	
	// A)
	
	// La implementación con array de colas FIFO aquí sugerida
	// Ya que el primero que entra es el mas antiguo, y es el que aparece al
	// llamar first()
	
	// El Monticulo no lo cumple, ya que el primero que sale es el ultimo que ha entrado.
	
	// b) 
	// Enqueue() => O(m), donde m es el numero total de pares <Key, Value> en la cola con prioridad.
	// Dequeue() => O(1)
	// first() => O(1)
	
}
